import Bloxorz._

object test {;import org.scalaide.worksheet.runtime.library.WorksheetSupport._; def main(args: Array[String])=$execute{;$skip(111); val res$0 = 
  terrainFunction(Vector(Vector('S', 'T'), Vector('o', 'o'), Vector('o', 'o')));System.out.println("""res0: <error> = """ + $show(res$0))}
}