import patmat.Huffman._

object work {import scala.runtime.WorksheetSupport._; def main(args: Array[String])=$execute{;$skip(78); 

  val listOfTrees = List(Leaf('a', 3));System.out.println("""listOfTrees  : List[patmat.Huffman.Leaf] = """ + $show(listOfTrees ));$skip(23); val res$0 = 
  combine(listOfTrees);System.out.println("""res0: List[patmat.Huffman.CodeTree] = """ + $show(res$0))}
}